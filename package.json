{
  "name": "cvf-api",
  "version": "1.0.0",
  "description": "API do Campus Virtual",
  "private": "true",
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
